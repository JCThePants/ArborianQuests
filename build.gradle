apply plugin: 'java'

sourceCompatibility = 1.5
version = '1.0'

def repositoryPath = 'C:\\JavaRepo'

repositories {
    mavenCentral()

    flatDir {
        dirs repositoryPath
    }
}

dependencies {
    compile files('bukkit/bukkit-1.7.9-R0.jar')
}